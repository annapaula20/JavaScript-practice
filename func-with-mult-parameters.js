function square(num) {
    console.log(num * num);
}
//multiple parameters but the order does not matter(output will be the same)

function sum(x, y) {
    console.log(x + y);
}

//sum (4,5) = 9 / sum(5,4)=p
//Mult. parameters and order matters as output will be diffferen
function divide(a, b) {
    console.log(a / b);
}
//divide (4,1) = 4 / divide (1,4) =.25